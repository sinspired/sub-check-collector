# 智能排序与过滤机制说明

## 🎯 核心目标

确保收集到的订阅链接来自 **高质量** 且 **活跃维护** 的仓库。

## 📊 排序算法

### 综合评分公式

```
综合得分 = (star权重 × 归一化stars) + (时间权重 × 归一化时间)
         = (0.7 × 归一化stars) + (0.3 × 归一化时间)
```

### 权重设计

- **Star 权重: 70%** - 体现仓库的流行度和可信度
- **时间权重: 30%** - 确保仓库保持活跃更新

### 归一化处理

两个维度都通过 Min-Max 归一化到 [0, 1] 区间:

```typescript
归一化值 = (当前值 - 最小值) / (最大值 - 最小值)
```

## 🔍 三级过滤机制

### 1️⃣ 第一级: API 搜索

- 使用 GitHub API 搜索关键字
- 按更新时间排序获取结果
- 获取 3 倍数量用于后续过滤 (最多 100 个)

### 2️⃣ 第二级: Star 过滤

- 过滤掉 star 数量低于阈值的仓库
- 默认阈值: `0` (不过滤)
- 可配置: `MIN_STARS` 环境变量

示例:

```env
MIN_STARS=5  # 只保留 star >= 5 的仓库
```

### 3️⃣ 第三级: 时间过滤

- 过滤掉超过指定天数未更新的仓库
- 默认阈值: `90` 天
- 可配置: `MAX_DAYS_SINCE_UPDATE` 环境变量

示例:

```env
MAX_DAYS_SINCE_UPDATE=30  # 只保留 30 天内更新的仓库
```

## 📈 工作流程

```
GitHub 搜索
    ↓
获取 3× 数量结果 (最多 100)
    ↓
过滤: star >= MIN_STARS
    ↓
过滤: 更新时间 <= MAX_DAYS_SINCE_UPDATE
    ↓
综合排序 (star 70% + 时间 30%)
    ↓
取前 N 个 (MAX_REPOSITORIES)
    ↓
提取订阅链接
```

## 🎨 配置示例

### 默认配置 (宽松)

```env
MIN_STARS=0
MAX_DAYS_SINCE_UPDATE=90
```

### 推荐配置 (平衡)

```env
MIN_STARS=5
MAX_DAYS_SINCE_UPDATE=60
```

### 严格配置 (高质量)

```env
MIN_STARS=10
MAX_DAYS_SINCE_UPDATE=30
```

### 极致配置 (顶级)

```env
MIN_STARS=50
MAX_DAYS_SINCE_UPDATE=14
```

## 📊 输出示例

运行时会显示详细的过滤信息:

```
🔍 搜索关键字: free v2ray
   最低 star: 5
   最大更新天数: 60 天
✅ 初步找到 100 个仓库
   ⭐ 过滤 star < 5: 100 → 75
   📅 过滤超过 60 天未更新: 75 → 50 (过滤了 25 个)
🎯 最终选择 30 个仓库
```

## 🧮 评分示例

假设有 3 个仓库:

| 仓库 | Stars | 最后更新 | 归一化Star | 归一化时间 | 综合得分 | 排名 |
|------|-------|----------|-----------|-----------|---------|------|
| A    | 100   | 1天前    | 1.0       | 1.0       | 1.0     | 🥇  |
| B    | 50    | 30天前   | 0.5       | 0.5       | 0.5     | 🥈  |
| C    | 10    | 60天前   | 0.0       | 0.0       | 0.0     | 🥉  |

计算过程:

- **仓库 A**: (1.0 × 0.7) + (1.0 × 0.3) = 1.0
- **仓库 B**: (0.5 × 0.7) + (0.5 × 0.3) = 0.5
- **仓库 C**: (0.0 × 0.7) + (0.0 × 0.3) = 0.0

## 💡 使用建议

### 1. 首次运行

建议使用默认配置 (宽松),收集更多链接:

```env
MIN_STARS=0
MAX_DAYS_SINCE_UPDATE=90
```

### 2. 日常使用

使用推荐配置,平衡质量和数量:

```env
MIN_STARS=5
MAX_DAYS_SINCE_UPDATE=60
```

### 3. 追求质量

使用严格配置,只要最优质的:

```env
MIN_STARS=10
MAX_DAYS_SINCE_UPDATE=30
```

## ⚙️ 调优建议

### 如果结果太少

- 降低 `MIN_STARS` (比如从 10 → 5)
- 增加 `MAX_DAYS_SINCE_UPDATE` (比如从 30 → 60)
- 增加 `MAX_REPOSITORIES` (比如从 30 → 50)

### 如果结果太多/质量不高

- 提高 `MIN_STARS` (比如从 0 → 5)
- 减少 `MAX_DAYS_SINCE_UPDATE` (比如从 90 → 60)
- 减少 `MAX_REPOSITORIES` (比如从 30 → 20)

## 🔧 技术细节

### 为什么选择 70/30 权重?

- **70% Star**: Star 数量是仓库质量的重要指标
- **30% 时间**: 保证仓库活跃,但不过度强调

### 为什么获取 3 倍数量?

- GitHub API 限制每页最多 100 个
- 经过过滤后,可能只剩下 1/3 的仓库
- 获取 3 倍确保有足够的候选仓库

### 为什么默认 90 天?

- V2Ray 订阅源更新频率一般为 1-3 个月
- 90 天是一个合理的活跃度指标
- 太短会过滤掉很多有效仓库
- 太长会包含过期无效的仓库

## 🎉 优势

1. **智能平衡**: 同时考虑流行度和活跃度
2. **灵活配置**: 可根据需求调整过滤条件
3. **可视化反馈**: 显示详细的过滤过程
4. **性能优化**: 先过滤后排序,减少计算量
5. **质量保证**: 多级过滤确保链接质量
